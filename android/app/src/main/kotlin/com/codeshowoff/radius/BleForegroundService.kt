package com.codeshowoff.radius

// Foreground-only mode: advertising foreground service removed.
//
// TODO(foreground-only): Restore the original foreground service if background
// advertising is intentionally reintroduced.
